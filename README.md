# Pokemon FastAPI

A FastAPI application to manage and view Pokemon.

## Features
- FastAPI backend (`main.py`, `models.py`, `database.py`, `services.py`)
- SQLite database
- Frontend templates (`templates/`)

## How to run

1. Install requirements
2. Run the application using Uvicorn:
   ```bash
   uvicorn main:app --reload
   ```
